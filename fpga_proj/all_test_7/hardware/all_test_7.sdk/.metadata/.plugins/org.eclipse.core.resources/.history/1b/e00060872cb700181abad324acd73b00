#include "xparameters.h"
#include "xil_io.h"
#include <stdio.h>
#include <time.h>

//#define MY_PWM XPAR_MY_PWM_CORE_0_S00_AXI_BASEADDR //Because of a bug in Vivado 2015.3 and 2015.4, this value is not correct.
#define MY_STEP 0x43C00000 //This value is found in the Address editor tab in Vivado (next to Diagram tab)

int main(){
    int num=0;
    int i;

    Xil_Out32(MY_STEP+8,31250);
    Xil_Out32(MY_STEP+12.62500);


    	xil_printf("pulse = %d\n",Xil_In32(MY_STEP));
    	xil_printf("Z Flag = %d\n",Xil_In32(MY_STEP+4));
    	xil_printf("duty =%d, period = %d",Xil_In32(MY_STEP+8),Xil_In32(MY_STEP+12));
    	Xil_Out32(MY_STEP+16,0);

    	sleep(10);

    	Xil_Out32(MY_STEP+16,1);

    	while(1);


}
